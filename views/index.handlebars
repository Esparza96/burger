<h1>Burgers!</h1>

<h2>Burgers that i will soon eat!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burgers-block devoured=false}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers i ate!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
     <li> {{ burger_name}} </li>
    {{/if}}
  {{/each}}
</ul>

<h2>Add a burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burger">burger Name:</label>
    <input type="text" id="burger" name="name">
    <button type="submit" id="addBurger" > Eat the burger</button>
  </div>
</form>
